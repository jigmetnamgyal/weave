// Package runner will supervise the sandbox-side agent process and provider
// adapters inside a session's isolated environment.
//
// Scaffold only: no logic until M5 (isolated runner).
package runner
