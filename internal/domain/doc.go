// Package domain will hold provider-independent entities, value objects,
// policies and state transitions.
//
// Domain code must not import web frameworks, SQL drivers, provider SDKs,
// queue clients or cloud SDKs. Scaffold only: no logic until M2.
package domain
