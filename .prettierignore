node_modules/
.next/
out/
build/
coverage/

# Generated shadcn/ui primitives are not edited by hand, and that includes
# reformatting them. See context/code-standards.md (Styling).
apps/web/components/ui/

# Authored specification documents. Their line wrapping is deliberate and
# rewrapping them produces large, meaningless diffs.
context/

# Types generated from contracts/openapi/openapi.yaml by `make contracts`.
#
# Not merely "generated, so leave it alone": formatting it would fight the
# generator directly. Prettier would rewrite the file, the next regeneration
# would undo that, and the drift check would then fail on every run for a
# reason that has nothing to do with the contract.
apps/web/lib/api-types.gen.ts

# Lockfiles and generated manifests
package-lock.json

# Go owns its own formatting via gofmt.
*.go
go.sum
