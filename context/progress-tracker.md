# Progress Tracker

Update this file after every meaningful implementation change. It is the concise handoff document for continuing development across sessions. Do not use it as a substitute for detailed issues, ADRs, or runbooks.

## Current Phase

- **Phase 1 — Engineering foundation**
- Status: M5.2 merged; M5.3 next; M4.3's browser walk still outstanding

## Current Goal

Make a session actually run: drain the outbox, start a durable workflow, cut a branch, and provision an isolated runner for a deterministic provider. M4 left every session sitting in `queued` with a promise nothing reads.

## Product Milestones

| Milestone | Outcome | Status |
| --- | --- | --- |
| M0 | Product, UI, architecture, standards, and AI workflow specifications accepted | Complete |
| M1 | Monorepo, local infrastructure, CI, observability bootstrap, and environment validation | Complete |
| M2 | Authentication, workspaces, membership, authorization matrix, and tenant isolation | Complete |
| M3 | GitHub App installation, repository access, webhook ingestion, and branch operations | Complete |
| M4 | Task model, agent profiles, provider capabilities, and session creation | Complete, pending the M4.3 browser walk |
| M5 | Durable session workflow, runner manager, isolated runner, and fake provider adapter | In progress |
| M6 | Claude Code adapter, normalized events, live session room, and reconnect | Not started |
| M7 | Approval policy, tool proxy, diff review, verification, and revision loop | Not started |
| M8 | Codex adapter, commit/pull-request delivery, usage ledger, and quotas | Not started |
| M9 | Billing, production hardening, security review, runbooks, staging, and launch readiness | Not started |

## Completed

- Built branch creation in the session workflow: a `CreateSessionBranch` activity that cuts the branch and a `RecordSessionBranch` activity that records it, between provisioning and the (still unavoidable) failure, a system entry point `CreateBranchAsSystem`, a write-once `sessions.branch_sha` recorded in the **same transaction** as its audit row, a `FailSession` activity that ends the session with a reason naming the cause, and workflow version 2 gated so version-1 executions finish on their original path (Unit M5.2 — see Verification Record). **A session now leaves a real branch behind** — verified against GitHub faked at the HTTP boundary and then live: the first branch Weave has ever created in a real repository.
- Built the outbox publisher and the first durable workflow: a fenced claim protocol with a terminal outcome, a `services/worker` process running both the Temporal worker and the publisher, and a session workflow that moves `queued → provisioning → failed` on its own as the system. **A session created through the API now reaches a terminal state with no human action** (Unit M5.1 — see Verification Record).
- Built idempotency keys: `idempotency_keys` with row-level security in the creating migration, a claim that commits before the work and carries a lease, a response recorded inside the work's own transaction, an `Idempotency-Key` header described by the contract and sent by the web form with a key stable across retries of one submission (Unit M5.0 — see Verification Record). **The gate M4.2's review put in front of the rest of M5.**
- Built the task, agent and session surface: pages for tasks, agents and sessions under a workspace, forms for all three, a task ready/draft toggle, and twelve API client functions — every type and envelope from the generated contract, none hand-written. The placeholder card on the workspace page is replaced by the way in. **Verification unit**: it exists so M3.3, M4.1 and M4.2 can be checked by a person rather than only by `curl` (Unit M4.3 — see Verification Record).
- Built session creation (merged 2026-09-16 in PR #10 after one review round and six findings): `sessions`, `session_participants`, append-only `session_state_transitions` and `outbox_events`, all with row-level security in the creating migration; the sixteen-state machine as a transition table with every pairing decided; a branch name derived from the session id rather than generated; and `CreateSession` writing the session, participant, first transition, audit event and outbox promise in one transaction (Unit M4.2 — see Verification Record). **Completes M4.**
- Built the task and agent model: `tasks`, `agents` and append-only `agent_versions`, all with row-level security in the creating migration; a closed capability set the database also enforces; task bodies treated as untrusted input and returned byte-identical; and the permission decision recorded — `session:create` for tasks, `workspace:manage` for agent profiles, both reused rather than inventing permissions nothing distinguishes (Unit M4.1 — see Verification Record).
- Built branch operations: name generation and Git-rule validation in the domain, ref reading and creation in the GitHub adapter, and a service that re-checks reach against GitHub before writing (Unit M3.3 — see Verification Record). Merged 2026-09-15 in PR #8 after five review rounds and ten findings, three of which were regressions introduced by the preceding round's fix — all three in the same seam, what happens when the external write succeeds and the local record does not. **Completes M3.**
- Generated the web application's API types from `contracts/openapi/openapi.yaml` with a CI drift check, replacing every hand-written response type and envelope in `apps/web/lib/api.ts` and adding a method-aware route-coverage test in both directions (Unit M3.2 — see Verification Record). Merged 2026-09-13 in PR #7. Closes the exception accepted on PR #6, and was done before M3.3 so that repository operations migrate two layers of hand-written helpers rather than three. **Deliberate interpretation, recorded rather than left implicit:** it generates types, not a request client — `Result<T>` stays, because it makes failure unignorable where a client that throws would not. **Not closed:** nothing verifies the contract against what the handlers actually serve; route coverage is the cheap half, and shape verification is not attempted.
- Built GitHub App installation and repository access: `github_installations`, `repositories`, `repository_permissions` and a delivery log, all with row-level security in the creating migration; installation binding authorized by a single-use state recorded before the redirect; signature-verified, deduplicated webhooks; reconciliation before use; and a health check that names the missing permission (Unit M3.1 — see Verification Record and ADR-005). Merged 2026-09-13 in PR #6 after five review rounds and ten findings, five of which were regressions introduced by fixes in the preceding round — every one in the webhook-delivery path.
- Built row-level security: a non-owning `weave_app` role the API connects as, transaction-scoped tenant context via `SET LOCAL`, forced policies on `workspaces`, `workspace_members`, `workspace_invitations` and `audit_events`, and two `SECURITY DEFINER` functions for the lookups that legitimately cannot be workspace-scoped (Unit M3.0 — see Verification Record and ADR-012). Merged 2026-09-11 in PR #5 after two review rounds and eleven findings, including a live defect that made the invitation preview 404 for every legitimate invitee. Shipped ahead of ADR-010's stated gate because M3.1 roughly doubles the tenant-owned surface.
- Defined developer-first MVP and long-term multiplayer-agent product boundary.
- Defined modular control plane and isolated execution plane.
- Selected initial technology stack and service boundaries.
- Defined provider-neutral adapter strategy for Claude Code and OpenAI Codex.
- Defined session lifecycle, approvals, tenant model, runner security, and reliability invariants.
- Defined UI design system and flagship shared-session experience.
- Defined repository code standards and AI implementation workflow.
- Installed and configured shadcn/ui design system and UI primitives (see Verification Record).
- Built the repository foundation: monorepo layout, Go API with health probes, local Docker Compose stack, task runner, environment validation, CI quality gates, and OpenTelemetry bootstrap (Unit M1.1 — see Verification Record).
- Built workspace invitations: `workspace_invitations` with hashed single-use tokens, seven-day expiry, issue/list/revoke/accept, the M2.2 grant rule extended to invitations, and the invite and accept surfaces in the web application (Unit M2.3 — see Verification Record and ADR-011).
- Built workspaces and membership: `workspaces`, `workspace_members` and append-only `audit_events`; the owner/admin/developer/viewer role model behind a centralised action-oriented permission matrix; two-step authorization (membership decides visibility, permission decides the operation); workspace-scoped data access with no unscoped reads (Unit M2.2 — see Verification Record). Merged 2026-09-10 in PR #3 after eight review findings were fixed, including a privilege-escalation path and a CHECK constraint that `citext` had silently disabled.
- Built the authentication baseline: database foundation (goose migrations, sqlc, `users` table), the identity-verifier port with a Clerk JWKS adapter, deny-by-default auth middleware, just-in-time user provisioning, `GET /v1/me`, the first OpenAPI document, and Clerk sign-in with a protected route in the web shell (Unit M2.1 — see Verification Record). Verified end to end on 2026-09-10: sign-in with GitHub through a real Clerk application provisions one `users` row and `GET /v1/me` returns it.

## In Progress

Nothing in progress. M5.2 merged as 29bbba1 (PR #17) after two review rounds; the four decisions it was required to record are under Session Notes and the review rounds are in the Verification Record. M5.1 merged as aa59c70 (PR #16); the five decisions it was required to record are in the Verification Record below. M4.3's signed-in browser walk is still outstanding and still needs a GitHub sign-in only the operator can perform — and M3.0's verification has never been recorded either, which the same session would close.

## Next Up

### M5, split into six units

M5's line is "durable session workflow, runner manager, isolated runner, and fake provider adapter" — four deployment units and a security boundary. M3 was split into four units for less, and M4 into three.

**A correction to this file's own gating, found while planning.** Three Open Questions below are marked "Required before M5": runner isolation technology, source retention, and the default egress policy. That is too coarse. **None of them blocks M5.0 through M5.3** — idempotency, the publisher, the workflow and the event pipeline touch no sandbox. They block **M5.4**, where a runner is actually provisioned. The questions are re-gated accordingly, so the milestone is not held up by decisions its first four units do not need.

**M5.0 — Idempotency keys.** Specced in `context/features-specs/13-idempotency-keys.md`. The gate M4.2's review put in front of everything else: today a lost response on `POST /sessions` duplicates a row, and the moment M5.1 exists the same retry starts two workflows and cuts two branches. The mechanism is built generally and applied to `createSession` only — most of the other nine mutations are already idempotent for reasons worth keeping rather than overriding.

**M5.2 — Branch creation in the workflow.** Merged as 29bbba1 (PR #17). Specced in `context/features-specs/15-branch-creation-in-the-workflow.md`. The first real call to `CreateBranch`, as an activity with bounded retries. **Closes the oldest outstanding gap in the project**: M3.3 shipped in September and no branch has ever been created against a real repository. The branch name is already decided and stored by M4.2, derived from the session id precisely so a retried activity finds the branch it made rather than cutting a second.

**M5.3 — Event contracts and ingestion.** `contracts/events` is a placeholder that names M5 as its occupant. Schemas, NATS JetStream, and the ingestor that validates, deduplicates, sequences and persists. Needed before a runner has anywhere to send anything, and separate from the runner because at-least-once delivery and ordering are their own problem.

**M5.4 — Runner manager and the isolated runner.** The security boundary, and the unit the three Open Questions actually gate. `context/architecture.md` is specific and unusually long here — one ephemeral sandbox per session, read-only base image, non-root, dropped capabilities, gVisor or microVM, deny-by-default egress, no host Docker socket. Expect this to need splitting again once it is specced properly.

**M5.5 — The fake provider adapter.** Deterministic, and a first-class provider rather than a placeholder: the Session Notes require it so orchestration, events and approvals are verified before a paid provider is wired in. It is what makes the whole chain demonstrable.

## Open Questions

Resolve these before the milestone that depends on them:

- ~~**`make ci` is not a superset of what CI runs**~~ — **Resolved 2026-09-14 in M3.3: the tidy check was added to `make ci`.** The alternative was to stop calling the target "every local quality gate", which would have been honest labelling of a gate that still let a known CI failure through. A local gate exists to catch things before CI does, so the fix was to close the gap rather than document it. `tidy-check` runs `go mod tidy` and fails on a diff, the same shape as `sqlc-check` and `contracts-check`.

- **The generated OpenAPI client does not exist.** `context/code-standards.md` requires one — "use the generated API client; do not call control-plane endpoints through scattered ad hoc `fetch` helpers" — and `apps/web/lib/api.ts` has hand-written helpers and response types for the whole `/v1` surface. The contract itself is now complete and current; what is missing is generation. Closing it means choosing a generator, wiring a drift check into CI so the client cannot silently diverge, and migrating every call site from M2.1 onward. Raised on PR #6 and deliberately not done there: it predates the GitHub endpoints and is wider than any one feature. **Being closed now as Unit M3.2** — see `context/features-specs/08-generated-api-client.md`.

- **Two standards in `context/code-standards.md` are unmet project-wide, raised on PR #10 and verified as real rather than paraphrased.**

  **Cursor pagination** (line 98: "Use cursor pagination for unbounded collections"). The contract has **11 list operations and none is paginated**; 14 list queries carry `ORDER BY` and one carries `LIMIT`. Sessions and transition histories grow fastest, so M4.2 makes it more visible, but it did not introduce it — workspaces, members, invitations, repositories, tasks and agents are all unbounded too. Fixing it properly is one change across the contract, the generated client and every list endpoint, which is why it is not folded into a session unit. **Gate: before the first workspace with real volume, and before M6's session room, which reads histories continuously.**

  **Idempotency keys** (line 100: "Mutations that may be retried require an idempotency key and a documented idempotency scope"). Nothing in the project has one, and `idempotency_keys` is in the architecture's table list but unbuilt. `POST /sessions` is the sharpest case: a lost response today duplicates a row and an outbox record, which is recoverable — but once M5's publisher exists, the same retry starts **two workflows and cuts two branches**, and invariant 5 says every external side effect is idempotent. **Gate: idempotency lands before M5's outbox publisher, because that is the commit that turns a duplicate row into a duplicate branch.**

- **`weave_app` can delete a workspace, and that cascade reaches append-only history.** Raised while fixing a narrower version of it on PR #10. DELETE on `sessions` was withheld there, because deleting a session cascades to `session_state_transitions` and the append-only trigger permits that cascade by design — so the grant was a route to erasing history with nothing raising an error. The same path still exists one level up: migration 00004 grants `weave_app` DELETE on `workspaces`, which cascades to sessions and then to transitions. Nothing in the application deletes a workspace — there is no query, repository method or route — so the grant appears to be unused. Revoking it touches another unit's migration and the tests that cover it, and needs a decision about how a tenant is deleted when that is eventually offered. **Gate: before the first external customer**, alongside anything else that can erase an audit or history trail.

- **List envelope keys are inconsistent across the API.** Six list operations return `{ items: [...] }` and five return a named key — `repositories`, `installations`, `sessions`, `transitions`, `participants` — which is the eleven list operations accounted for. It has been that way since M3.1 and was found in M4.3, when one client consumed all of them for the first time and failed to compile. Nothing is broken; a reader of the contract cannot predict the shape, and every new list endpoint has to guess which convention to follow. Aligning them touches ten operations, their handlers, their tests and the generated client. **Gate: whenever the pagination work happens**, since that rewrites every list envelope anyway and doing both at once costs almost nothing extra.

- **M3.0's browser verification was never closed.** Its Verification Record row ends "Gap: the signed-in browser flow remains unverified", and the 2026-09-11 walk that closed the equivalent gaps for M2.2, M2.3 and M3.1 does not name it. Row-level security is exercised by every signed-in request, so it was almost certainly covered in practice — but nothing recorded that, and an unrecorded verification is an unverified one. Found on PR #13 while a reviewer was checking a different claim. **Gate: fold it into the outstanding M4.3 walk**, which is a signed-in session anyway, rather than making a separate occasion of it.

- **The API and the worker share one database role, so a compromised API can reach the cross-workspace outbox claim.** Raised on PR #16 and worth keeping rather than acting on immediately: the reviewer's own conclusion was that the grant matches this project's documented contract — the publisher operation runs as `weave_app`, which is what the spec says — so this is a post-compromise least-privilege concern, not a reachable authorization bypass. No API path calls `weave_claim_outbox_batch`, and `TestNoHandlerActsAsTheSystem` guards the neighbouring shortcut. A dedicated `weave_worker` role with execute on that function and nothing else would close it. **Gate: production hardening in M9**, alongside the other role and deployment-boundary decisions, or earlier if the deployment requires the API and worker to be isolated from each other.

- **A session's own branch becomes a conflict if the base moves between the cut and its completion.** Found building M5.2, raised again on PR #17, and **narrowed there**. `CreateBranch` is idempotent by *name and base commit*. The first shape re-ran the cut whenever *recording* failed, so a database hiccup plus a push to the base was enough to fail a session over a branch it had made itself. Since the review round, cutting and recording are separate activities and the cut's result is durable in workflow history, so a failed record never re-asks GitHub. What remains is the cut activity's own completion being lost — the worker dying between GitHub's response and Temporal hearing of it — followed by a push to the base before the retry. Safe when it happens: the session fails loudly and nothing is overwritten. The fix is to pin the base commit on the session **before** the external write and cut at that commit. **Gate: M5.4**, which checks out that commit and is the first unit to depend on the base being exactly what was cut from.

- **Cancelling a session does not yet remove its branch, and can race the cut.** Raised on PR #17. `CutBranch` checks the session is `provisioning` before calling GitHub; a cancel landing during the GitHub calls cannot stop a ref that is already being made, and `RecordBranch` records the SHA regardless of state — deliberately, because a ref that exists is a fact, and refusing to record it would leave a branch nobody knows about. **Not reachable today:** no API route calls `SessionService.Transition`, and the `transitions` route is read-only, so nothing can cancel a session. The complete answer is not a tighter check before the write — no check can be atomic with GitHub — but cleanup after it: the cancellation path must delete the session branch when `branch_sha` is recorded and the branch has not moved from it. **Gate: the unit that first routes cancellation**, which must own that cleanup.

- **Integration tests and a running `make dev` worker claim each other's outbox rows.** Seen on PR #17's review round. The outbox claim is global by design (M5.1), so a dev worker left running while `make test-integration` runs claims the tests' sessions and runs them on its own queue — here against real GitHub with a fake repository, failing the tests as "unreachable". M5.1 fixed the other direction (the tests putting back rows they did not create) but not this one. Workaround: stop or pause `make dev` while running integration tests. A real fix is a test-only outbox topic or workspace filter, or refusing to start the harness when another worker holds the queue. **Gate: before CI runs integration tests beside anything else that drains the outbox.**

- **An uninstalled App reads as "GitHub unreachable" rather than "repository unavailable".** Found building M5.2. The GitHub port translates `ErrNotFound` to `application.ErrRemoteNotFound` for branches but not for `Installation()`, so an installation deleted on GitHub (webhook not yet processed) surfaces as an untranslated error, is retried five times, and fails the session with the unreachable reason. The outcome is right and the wording is wrong. **A sibling, seen live:** an empty repository reports *"the base branch does not exist"*, which is accurate but does not tell a person that the repository has no commits at all — worth distinguishing when these reasons reach the session room. Translating it is a one-line change to a port M3.1 owns, with its own tests, so it is recorded rather than folded in. **Gate: before M6's session room shows these reasons to people.**

1. **Commercial name:** confirm trademark and domain viability for “Weave.” Required before public launch, not before engineering foundation.
2. ~~**Identity provider**~~ — **Resolved 2026-09-10: Clerk**, behind the OIDC/JWT boundary, for authentication only. Clerk Organizations is explicitly not used; workspaces, membership and every permission decision stay in PostgreSQL, because two sources of truth for authorization is how tenant isolation breaks. Chosen over Auth.js because the Next.js/Go split needs a token the Go service can verify via JWKS, and hand-rolling that issuance is custom crypto across a security boundary; chosen over WorkOS because SAML and SCIM are deferred. See ADR-009. **Still outstanding:** confirm current Clerk pricing against projected workspace count — seats are billed per user while Weave bills per workspace, so model the mismatch before launch, not before M2.1.
3. **Runner isolation:** select managed microVM provider versus Kubernetes with gVisor/Firecracker based on team operations capacity, region availability, cold-start target, and cost. **Required before M5.4**, not before M5 — the publisher, workflow and event pipeline provision nothing.
4. **Provider licensing and automation:** confirm current Claude Code and Codex terms, authentication flows, headless interfaces, redistribution constraints, and organization billing. Required before production adapters in M6/M8.
5. **Source retention:** decide whether repository snapshots are retained after session completion or deleted while retaining patches/logs. **Required before M5.4**, when a checkout first exists.
6. **Default egress policy:** define minimum domains for language package registries and repository builds, including whether customers configure allowlists. **Required before M5.4**, when there is first a sandbox to police.
7. **Approval defaults:** confirm which commands and tools are auto-allowed for the first repository types. Required before M7.
8. **Initial hosting region:** choose based on customer location, isolated-compute availability, provider connectivity, and data policy. Required before staging.
9. **Pricing units:** choose included agent time, provider pass-through, or customer-supplied provider account. Required before M8/M9.

## Architecture Decisions

| ID | Decision | Reason | Status |
| --- | --- | --- | --- |
| ADR-001 | Begin with a modular Go control plane and separate execution plane | Keeps product logic cohesive while isolating untrusted code | Accepted |
| ADR-002 | Use PostgreSQL as product source of truth | Strong transactions, mature operations, and tenant controls | Accepted |
| ADR-003 | Use Temporal for session workflows | Durable timers, signals, retries, cancellation, and recovery for long-running sessions | Accepted |
| ADR-004 | Use NATS JetStream for runner event transport | Durable at-least-once transport and horizontal fan-out | Accepted |
| ADR-005 | Use GitHub App credentials, and bind an installation to a tenant by state recorded before the redirect | A PAT carries one person's access, lives until revoked, and is invisible to the organisation; an App installation is granted, scoped, revocable and auditable by the account itself. The private key is environment configuration and never database content; installation tokens are cached in Redis and never persisted. GitHub returns an installation id and nothing naming the workspace, so the workspace comes from a single-use value recorded before the redirect, never from the callback. See `docs/adr/0005-github-app-credentials.md` | Accepted |
| ADR-006 | Use one ephemeral isolated environment per session | Strong tenant boundary and deterministic cleanup | Accepted |
| ADR-007 | Keep provider behavior behind capability-aware adapters | Avoid product coupling to Claude Code or Codex | Accepted |
| ADR-008 | Bind approvals to immutable proposal hashes | Prevent replay or parameter substitution | Accepted |
| ADR-011 | Treat invitations as bearer capabilities, storing only a SHA-256 of the token | A link is held by whoever receives it, so the token is 256 random bits, single use, expiring, and matched against the invited address; hashing means a database dump is not a set of working invitations. A password hash would buy nothing against uniform randomness and would put a CPU cost on every acceptance. See `docs/adr/0011-invitation-token-model.md` | Accepted |
| ADR-010 | Enforce tenancy with workspace-scoped queries; defer row-level security | RLS is defence in depth behind scoped queries, and needs a non-superuser role, a per-transaction tenant GUC and pooling care that would make this unit unreviewable. Gated: **RLS ships before the first external customer.** See `docs/adr/0010-tenancy-and-row-level-security.md` | Accepted; superseded in part by ADR-012 |
| ADR-012 | Row-level security enforced through a non-owning application role, transaction-scoped context, and fail-closed policies | `FORCE` does nothing against a superuser or `BYPASSRLS` role, so what carries isolation is the API connecting as `weave_app`, which is neither. Context is set with `SET LOCAL` because a session-scoped `SET` would hand one request's tenant to the next borrower of a pooled connection. Absent context matches no row: RLS that fails open looks like protection and is not. See `docs/adr/0012-row-level-security-implementation.md` | Accepted |
| ADR-009 | Use Clerk for authentication only, never for authorization | The Go API can verify Clerk JWTs via cached JWKS without hand-rolling token issuance across the Next.js/Go boundary; keeping workspaces, membership and permissions in PostgreSQL avoids a second source of truth for tenant isolation and keeps Clerk swappable behind the OIDC/JWT boundary | Accepted |

## Risks

| Risk | Impact | Mitigation | Owner | Status |
| --- | --- | --- | --- | --- |
| Provider CLI terms or interfaces change | Core adapter disruption | Versioned adapters, capability negotiation, contract tests | Unassigned | Open |
| Untrusted repository escapes sandbox | Critical security incident | MicroVM/gVisor isolation, no host socket, egress deny, security testing | Unassigned | Open |
| Runner cost exceeds willingness to pay | Poor unit economics | Usage ledger, quotas, warm-pool measurement, pricing experiments | Unassigned | Open |
| Noisy agent output overwhelms users | Low product usability | Normalized events, summaries, grouping, progressive disclosure | Unassigned | Open |
| Realtime and durable state diverge | Incorrect user decisions | PostgreSQL authority, sequence replay, idempotent reducer | Unassigned | Open |
| Broad product scope delays launch | No customer validation | Enforce developer-only MVP and milestone gates | Unassigned | Open |

## Verification Record

| Date | Unit | Environment | Commands/tests | Result | Notes |
| --- | --- | --- | --- | --- | --- |
| 2026-09-15 | Session Creation, Unit M4.2 (`context/features-specs/11-session-creation.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-down` / `migrate-up`; live `curl` of the five new routes unauthenticated; each new gate broken on purpose before being trusted | Pass, with one designed-but-unexercised gap | **The two decisions the spec required, made before the tables were written.** The pin *is* the policy snapshot: `agent_versions` is already append-only and immutable, so a snapshot table would hold a byte-for-byte copy of a row that cannot change — not a second record of the truth but a second thing that could disagree with it. It earns a table when a session's policy can be narrower than its agent's, which is M7 and is information that exists nowhere else. And the outbox claim is a **lease with an expiry** rather than a boolean, because a flag set by a publisher that then dies is a row nothing will ever touch again and no operator would know; all five outcomes were written down in this file before the columns existed. **A defect the rollback test caught immediately.** `Create` assigned the session before the audit write, so a failure after it returned a fully populated session — with an id — alongside the error, for a row the rollback had just removed. It returns the zero value now. This is the *opposite* call to M3.3's, deliberately: there the branch really existed on GitHub before the audit failed, so returning it was the only way not to hide an irreversible write; here nothing happened, so saying something did is the false statement. **Every new gate was broken on purpose before being trusted.** Adding a seventeenth state fails three tests independently; splitting `Create` into two transactions — the naive implementation — fails the rollback test with `sessions has 1 rows after a failed create`; the M3.2 route-coverage gate named all five routes before the contract described them. **Tests:** all six writes land together and none survives a failure injected on the last of them (a blank audit action, which `audit_events_action_not_blank` refuses); the branch name is a pure function of the session id across fifty derivations and stable across reads; `session_state_transitions` refuses UPDATE, DELETE and TRUNCATE while deleting a workspace still works; a session cannot pin another workspace's task, agent version or repository, and each refusal is translated to not-found rather than relayed as a constraint violation; a viewer is refused creation and still reads; and the four new tables return nothing to `weave_app` with no context and nothing when another tenant names this workspace, with an owning-tenant control so the isolation assertions cannot pass vacuously. **Gap, designed and not exercised:** nothing claims an outbox row until M5, so no test has watched a lease expire or two claimers race. The protocol is in the migration; the evidence is not, and M5 owes both. |
| 2026-09-18 | Outbox Publisher and First Workflow, Unit M5.1 (`context/features-specs/14-outbox-publisher-and-workflow.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2, Temporal 1.25.2 | `make ci`; `make test-integration` (now passing `TEST_TEMPORAL_HOST_PORT`, so the workflow tests run rather than skip); both owed claim tests run against the implementations they reject; a live worker observed moving a real session | Pass | **M4.2's two test debts, paid and proven.** Against a read-then-write claim the race test reports `2 claims across two publishers for one row`; against a boolean flag rather than an expiring lease, `the row was not claimable after its lease expired — a dead publisher stranded it`. Both were watched failing before being trusted, which is the third unit running where that mattered. **Building the claimer surfaced what the design could not show.** The outbox had no fencing token — the identical hole M5.0 found in `idempotency_keys` — and nowhere to record "stop trying": the claim predicate is `completed_at IS NULL`, so a dead row could only be marked complete, which lies, or left pending, which retries forever. `claimant` and `terminated_at` close both, and **every** settling update is fenced rather than an enumerated two, because the review of the spec caught exactly the third one missing. **The publisher is the one privileged read**, because it polls every tenant and an absent tenant context matches no row; it goes through a `SECURITY DEFINER` function that bounds its own arguments, and every write after the claim runs in an ordinary tenant transaction scoped by the workspace **on the claimed row**, never from a caller. **The system actor is a distinct identity.** `authorizeActor` refuses a nil user, so the shortcut was to borrow the member who created the session; refused, because attributing an automated move to a person is a false statement in a trail that cannot be corrected by editing. Observed live: the two workflow transitions carry no actor and the creation transition still carries the person. **Readiness, settled after being carried since M1.1:** the publisher lives in the worker, so Temporal does not become a serving dependency of the API and its probe is left alone — the worker instead treats an unreachable Temporal as fatal at startup. **A defect in my own tests, measured.** The claim is global by design, so the integration tests were claiming rows belonging to real work — one run took an unrelated row from 0 attempts to 1 and left it leased, and with a ceiling of 10 a few runs would have terminated a legitimate row. The helper now puts back anything that was not its own. **End to end:** a session created through the store reached `failed` in 2.1 seconds with no human action, and a republish after the first execution **closed** started no second workflow — the case running-id uniqueness does not cover, and the one a reviewer had to point out. |
| 2026-09-25 | Branch Creation in the Workflow, Unit M5.2 (`context/features-specs/15-branch-creation-in-the-workflow.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2, Temporal 1.25.2; GitHub faked at the HTTP boundary behind the real client | `make ci`; `make test-integration`; `make migrate-down` and `migrate-up` round trip on 00010; three mutations run against the tests that should catch them; `govulncheck`; the test logs grepped for tokens and keys | Pass, with one check outstanding | **End to end:** a session created through the store reached `failed` on its own, with exactly one `POST /git/refs` for its branch, `branch_sha` equal to the base commit, a final reason naming the missing runner, and one `github.branch.created` audit row with no actor. A repository the fake no longer lists, while our table still read `granted`, failed the session with *"the repository is no longer granted to the GitHub App installation"* and created nothing. **Redelivery composed, not just the parts:** the activity run twice on the application role, with no ambient tenant, left one ref, one SHA, one audit row and two transitions — recording the branch invents no state change. **Watched failing:** removing the tenant context from the activity turns the redelivery test red with `SessionNotFound`, the exact silent-read M5.0 hit; removing the recorded-SHA short-circuit fails the unit test with five GitHub calls on a redelivery; attributing the audit to the session's creator fails both the unit and the integration test. A mismatched workspace/session pair is a non-retryable not-found with **zero** GitHub calls. `TestNoHandlerActsAsTheSystem` now also refuses `*AsSystem(` and `.EnsureBranch(` in API source, and a probe file calling `CreateBranchAsSystem` was confirmed to fail it. `go.mod` untouched, `toolchain` directive intact, `govulncheck` reports nothing reachable; zero matches for `ghs_`, `Bearer`, or key headers in the test logs. **Live, against real GitHub (2026-09-25, 10:30–10:31 UTC):** the operator created two sessions through the signed-in browser flow against `jigmetnamgyal/thuenlam`, and the running worker took both through the new path: a real installation token was minted, the installation and repository were reconciled (both rows' `updated_at` at 10:31:10, between the `provisioning` and `failed` transitions), `contents: write` was confirmed, and both sessions failed with *"the base branch does not exist"* — **correctly**: the repository is empty (size 0, no branches), and the second session named a base `test-branch` that does not exist either. **Then a successful live cut (11:31 UTC)**, after a one-line `README.md` was pushed to the repository's `main` (at `cd7807b`) with the operator's agreement: a session created through `SessionService.Create` as the workspace owner, on the application role — the service call the HTTP handler makes, run directly because no Clerk token was to hand — went `queued → provisioning → failed` in 5.2 seconds with no human action. GitHub then listed `weave/work-on-authentication-3xyp37ao` at `cd7807b3e7b0a6639d7ff590e595303ae6dd12a6`, **the same SHA recorded in `sessions.branch_sha`**; the final reason names the missing runner; and the audit trail reads `session.created` (the owner), then `session.transitioned`, `github.branch.created` (`created: true`, base `main`) and `session.transitioned`, **all three with no actor**. **The first branch Weave has created against a real repository**, closing the gap M3.3 left open in September. **Review round (PR #17, Greptile, five findings):** (1) a 47 MB `worker` binary committed by `go build ./services/worker` from the root — removed, and root-level `/api` and `/worker` ignored; (2) a branch GitHub made could end unrecorded and misreported as "GitHub unreachable" when recording exhausted the shared retry budget — **fixed by splitting cut and record into two activities**: the cut's result is durable in history, recording is database-only with unlimited attempts over an hour, and if it still cannot land the reason says the branch exists (`branch_unrecorded`); watched failing against a record step using the GitHub policy — `recording was attempted 5 times` and `fail causes = [github_unreachable]`; (3) a non-suspended 403 or a non-duplicate 422 was retried and reported as unreachable — **fixed**: the client raises `ErrForbidden` / `ErrUnprocessable`, the port translates both to `application.ErrRemoteRefused` on the branch path keeping GitHub's message, and the session fails as `github_refused`; the duplicate-ref 422 is still told apart; (4) moving base — narrowed by (2), remainder gated to M5.4; (5) cancellation racing the cut — not reachable today, gated to the unit that routes cancellation. Version 2's shape changed without a version bump, on the reasoning that it had never shipped and every execution that ran it had closed. **Second round (one finding, and a regression the first round introduced):** naming 403s as refusals made them terminal, and GitHub sends its rate limits as 403s too — so a session would have failed for good over a limit that resets. The client now checks for a rate limit before a plain 403 (`X-RateLimit-Remaining: 0`, `Retry-After`, a "rate limit" message, or any 429) and raises `ErrRateLimited`, which stays retryable; `TestARateLimitIsNotARefusal` fails in all four cases with the check disabled. The reviewer accepted both deferrals (cancellation cleanup, moving base). |
| 2026-09-17 | M5.0 review resolution (PR #14) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `psql` as `weave_app` against the privileged function; each fix measured before and after | Pass | Eight findings across two rounds, all valid. **Three were bugs, and two of them made this unit's own guarantee quietly false.** *The lease had no fencing token* — found independently by both reviewers. A holder that is slow rather than dead returns after its lease expired and another caller has reclaimed the key; completion and release matched on scope and key alone, so it would overwrite or delete the new holder's claim while both created sessions — the duplicate arriving through the recovery path. Every claim now issues a token and a fenced completion fails the transaction rather than committing under someone else's record. *The retention sweep deleted nothing and reported success*, measured before touching it: a background context has no tenant and `FORCE` RLS then matches no rows. Worth recording why it was not caught — the delivery sweep it was copied from works because `github_webhook_deliveries` has no RLS at all. Now a `SECURITY DEFINER` function, the shape ADR-012 settled on, granted `SELECT` on exactly the one column its `WHERE` reads because a delete must read before it removes. **Then the re-review found the fix had a hole:** that function deletes across every workspace as a privileged role and took any interval, so `interval '0'` would empty the table for every tenant and the next retry of anything would duplicate. Narrow in shape, wide open in range. A floor is enforced inside the function, raised rather than clamped. *An expired reclaim overwrote the stored fingerprint*, so a different request could take over a key and the mismatch check would never fire again. **Two were about the work's own honesty.** The idempotency key was generated during render in a component rendered from a Server Component — a hydration mismatch, and `crypto.randomUUID` needs a secure context the server render is not, so it was calling an API that is not there. And the equivalence test named a case it did not exercise, re-sending an identical body and asserting only `!= 422`, which would have passed if the fingerprint logic broke in the other direction; it now sends four genuine variants and asserts the exact status, verified by removing the normalisation and watching it fail. |
| 2026-09-17 | Idempotency Keys, Unit M5.0 (`context/features-specs/13-idempotency-keys.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-down` / `migrate-up`; the race test run against a check-then-insert implementation three times before being trusted | Pass | **The four decisions the spec required, made before the columns existed** and committed separately so the code followed them. *The claim commits before the work, with a 60-second lease.* The alternative cannot produce the refusal the unit exists to give: a claim inside the work's transaction is invisible until commit, so a concurrent caller blocks for the full duration of someone else's request and then finds it complete. *The response is recorded inside the work's transaction*, not after — a session committed with its response write failed would expire the lease and let the retry create a second session, the failure reappearing at the last step. The transport passes a `Render` closure the store calls inside the transaction, the shape `apply` and `event` already have. *`X-Request-Id` echoes the retry's own id*, because the header identifies the exchange being answered; the original is stored and named in the replay's log line, so an operator can get from one to the other without the response carrying an id matching no log line. *Retention is 24 hours*, sized to a request-timeout window rather than to GitHub's redelivery window, and shorter because the rows hold a response body carrying identifiers. **A fifth outcome the spec did not name:** the work failed, which releases the key at once rather than refusing a legitimate retry for a minute. **The race test was watched failing first, which is the only reason it is evidence.** Against a check-then-insert implementation it reports `2 callers were told to proceed, want exactly 1` on every run; the pool is warmed first, because M4.2's first concurrency test passed against unfixed code when pgxpool's lazy second connection outlasted the overlap. **Tests:** two concurrent claims produce one winner and one in-flight refusal; an expired lease is claimable again without anyone clearing it; a key reused with a different body is refused; the same key in another workspace is a different key; a released key lets a retry proceed; and the table returns nothing to `weave_app` with no context. At the transport: a request with no header is unchanged, an in-flight key gets 409 with `Retry-After`, a completed key replays the stored status and body bytes with the retry's own request id, equivalent bodies share a fingerprint (omitted and whitespace `base_branch` alike), and a malformed key is refused before any work. **Scope applied narrowly**, as the spec required: `createSession` only, because the other nine mutations are already idempotent for reasons worth keeping — a single-use token, a unique delivery id, idempotency by branch name. |
| 2026-09-16 | M4.3 review resolution (PR #11) | Local dev, macOS arm64, Node 23.11.0, Next.js 16.3.4 | `make ci`; `next build`; `tsc --noEmit` before and after switching the wrappers to generated operation types; recount of the contract's envelope keys | Pass | Six findings, all valid, all fixed — five in the first round and one on re-review, the transition attribution described at the end of this row. This row said "five" until the re-review's finding was folded in; a count written mid-pass is stale the moment the next reviewer reports. **Two were the unit's own carried-forward lessons, broken in the unit that quoted them.** The ready/draft toggle called its action from a click handler — `startTransition(async () => …)` — under a comment stating it was bound and not wrapped. That is the M3.1 defect verbatim (the action runs, Next does not treat the result as an action response, the router never re-renders, and a working button looks dead) *and* the "comment that defends wrong code" failure the spec carried from M4.1, in one component. `SyncRepositoriesButton` already had the correct shape and the correct comment four files away. Rewritten with `useActionState` and a bound action. **And the client wrappers asserted shapes instead of naming operations** — `apiRequest<Agent>` rather than `apiRequest<Ok<"createAgent">>` — in the unit whose whole purpose is consuming the generated contract. `createAgent` returns `{ agent, version }`, so `result.data.id` was `undefined`; nothing visible broke, which is why an unchecked assertion is worth refusing on principle rather than on symptoms. Converting all five wrappers made the compiler find it immediately, and the version number is now shown back after creating a profile, which the spec's walk asked for. **A gap against the walk:** the sessions list showed no participant and no transition — the two side effects of creation that prove the six writes landed together — and the client functions for both were written and never called. A session detail page now shows them. **Two smaller:** `toLocaleString()` in a Server Component renders the server's zone as though it were the reader's, now explicit UTC with the zone named (the pre-existing members page has the same bug and is left alone); and the tracker said four named envelope keys where there are five — recounted, 6 + 5 = 11 list operations. |
| 2026-09-16 | Task, Agent and Session Surface, Unit M4.3 (`context/features-specs/12-task-agent-session-surface.md`) | Local dev, macOS arm64, Node 23.11.0, Next.js 16.3.4 | `make ci` (fmt, lint, typecheck, contracts-check, build); `next build`; signed-out request of all three new routes against the running stack; comparison against the pre-existing members route | Pass in code, **one gap: the walk itself** | The browser can now reach what the last three units built — 15 of 35 endpoints before this, and the entire output of M3.3, M4.1 and M4.2 was `curl`-only. **The generated types earned themselves again**, which is the third time: the client failed to compile because tasks and agents return `items` while sessions return `sessions`. The envelope key is inconsistent across the API — six operations use `items` and five use a named key, and it has been that way since M3.1. This unit was the first code to consume all of them and therefore the first to notice. Not fixed here (it touches ten operations, their handlers and their tests, which is not a change to make inside a UI unit) and recorded as an Open Question. **Three things the spec asked for, each carried from a defect the 2026-09-11 walk found:** a failed request and an empty list never share a sentence, on every one of the three pages; the ready/draft toggle binds its action rather than wrapping it in a closure, which is what made a working button look dead in M3.1; and the session list says outright that `branch_name` is a branch that does not exist yet, because the name reads like a link and is not one. **Also:** `next_states` is rendered from what the server sends rather than from a second copy of the state machine, the task body is rendered as text with `whitespace-pre-wrap` and never as markup, and the agents page fetches versions per agent — N+1 by construction, named in the code rather than left to be discovered. **Verified without a session:** all three routes return 307 to sign-in rather than 404, so a new route is protected by where it sits; `next build` registers all four workspace routes. The `Not signed in` throw they log is inherited, not introduced — the pre-existing members route does exactly the same, measured. **The gap:** the five-step walk — write a task, define an agent, start a session, see it queued — needs a GitHub sign-in only the operator can perform. Claude's browser pane has no Clerk session and no Chrome extension is connected, so it cannot be driven from here. |
| 2026-09-16 | M4.2 review resolution (PR #10) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-down` / `migrate-up`; each fix run against the unfixed code first; `psql` probe of the application role's privileges | Pass | Six findings — four fixed, two deferred with gates. **The worst was a lesson already written down and not applied.** Both reviewers independently found that the task is read in one transaction and the session written in another, so a PATCH can flip it back to a draft in between and leave a queued session referencing a task that is not runnable, or a branch intent for a repository the task has moved off; the composite foreign key checks identity and workspace, never status. That is the same shape as the lost update M4.1 fixed in `TaskService.Update`, one unit later — and the spec being implemented carried the lesson. The task row is now locked and re-checked inside the creating transaction through a closure, so the rule stays in the application layer. With the check removed, the test reports a session really was created for a draft task. **The second was a gap against this unit's own spec.** `11-session-creation.md` lists "A transition against a stale version is refused" in its test list; the state machine was built, `ErrSessionVersionConflict` was declared, and nothing ever raised it — an error with no caller, which was the tell. `SessionStore.Transition` now locks the session, checks the observed version and then the state pair, updates state and version, and appends the history row in one transaction. No HTTP route reaches it — the spec allows one edge at the transport — but the mechanism exists rather than leaving an insert-only query that looks like the way to record a move. **The third was filed as trivial and was not.** Deleting a session cascades to `session_state_transitions`, and the append-only trigger *permits* that cascade by design, so granting `weave_app` DELETE on sessions was a route to erasing append-only history with nothing raising an error. Withheld — and the fix is narrower than it looks, which the migration now says outright: `weave_app` still holds DELETE on `workspaces` from migration 00004 and that cascade reaches the same rows, now an Open Question gated before the first external customer. **Deferred, both verified against `context/code-standards.md` rather than taken on the reviewer's paraphrase:** cursor pagination (line 98 — 11 list operations, none paginated, so this unit continued the gap rather than introducing it) and idempotency keys (line 100 — nothing in the project has one). Both are recorded with the commit that makes them urgent rather than as open-ended follow-ups. The maintainer reviewed and accepted both deferrals. **Also corrected:** I reported the reviewers as having found nothing, on the strength of a green check. A passing review check means the review ran, not that it was clean. |
| 2026-09-15 | Task and Agent Model, Unit M4.1 (`context/features-specs/10-task-and-agent-model.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-down` / `migrate-up`; direct `psql` probes of every constraint and trigger; live `curl` of the four new routes unauthenticated | Pass | **Permission decision, recorded as the spec required.** Tasks are governed by `session:create` and agent profiles by `workspace:manage`, both reused rather than adding `task:manage` and `agent:manage`. A task is the input to a session, so anyone who may start one must be able to describe the work — and the matrix grants `session:create` to exactly the three roles a `task:manage` would have gone to, which would make it a permission distinguishing nothing. An agent version carries the tool policy, which decides what an agent may do inside a customer's repository: that is configuration and a security control, and the matrix already limits it to owner and admin. The separation worth having is that a developer may run agents and not redefine what they are allowed to do, and a transport test asserts exactly that. **A design problem the append-only trigger surfaced.** Blocking every DELETE also blocks the cascade when a workspace is removed — which would make a tenant undeletable and, more immediately, break the integration harness, since `seedWorkspace` cleans up by deleting workspaces. The trigger now permits a delete only when `pg_trigger_depth() > 1` *and* the parent agent is already gone; both signals were measured against PostgreSQL rather than assumed (a direct delete reports depth 1 with the agent present, a cascade depth 2 with it absent), and requiring both means a future trigger deleting versions directly would still be refused. Append-only exists so a session's history cannot be rewritten; when the workspace and its sessions are going too, there is no history left to protect. **The M3.2 route-coverage gate caught all nine new routes** before the contract described them. Tests: the body survives a full HTTP round trip byte-identical through six hostile inputs, editing a profile leaves version 1 untouched field by field, UPDATE/DELETE/TRUNCATE are all refused against real PostgreSQL, an unknown capability is a 400 naming what it refused, and the three new tables return nothing to `weave_app` with no context and nothing when another tenant names this workspace. |
| 2026-09-15 | M4.1 review resolution (PR #9) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-down` / `migrate-up`; `psql` probes of the foreign-key actions and the current-version pointer; each behavioural fix run against the unfixed code first | Pass | Eight findings across two reviewers, six taken as given, one taken differently, one corrected on its way in. **Length bounds counted bytes.** PostgreSQL's `length()` counts characters and Go's `len()` counts bytes, so a 200-character CJK title was refused under a message saying it exceeded 200 characters while the column would have stored it. **`tool_policy` generated `Record<string, never>`** — a TypeScript type permitting no keys — so the browser could neither send nor read a policy while the column held arbitrary `jsonb`. It is also now required to be a JSON object at the boundary, since `json.RawMessage` and `jsonb` both accept `null`, a number or an array, and M7 is where a stored string would finally surface. **PATCH was replacing, not patching** — the worst of the eight. The contract requires only a title, so a client renaming a task sends only a title, and the handler built a whole task from the decoded body: an omitted body was wiped, an omitted `repository_id` cleared, an omitted status reverted a ready task to draft. All three were observed at the transport before the fix. A `string` cannot distinguish an omitted key from one sent as `null`, which is what made it destructive, so the fields carry presence as well as value — omitting `repository_id` leaves it, sending `null` clears it, which is how a ready task returns to a draft naming nothing. `TaskPatch` is its own schema now, because a PATCH schema requiring a title is what invited the bug. **Two constraints were weaker than their comments claimed.** `ON DELETE SET NULL (repository_id)` is not a `SET NULL` for a ready task — the new NULL violates `tasks_ready_has_repository`, so the delete aborts anyway; probing it showed a check-constraint violation naming a row the caller never touched, which is `NO ACTION` reached by accident and reported unclearly. The workspace cascade was never at risk under either, contrary to the finding's reasoning, and that was measured. The current-version pointer was keyed on the workspace, which stopped an agent pointing at another tenant's version but allowed a sibling's — a profile reporting a model and tool policy belonging to a different agent. Keyed on the agent now. **The disagreement, left unresolved for a human.** A reviewer read the unguarded read path as an authorization bypass and proposed gating reads on `session:create`. A viewer holds `workspace:read` and not `session:create`, so that would make the role blind to the work it was invited to watch. Reads are governed by `workspace:read`, now stated rather than implied — every role holds it, so nobody is denied today, but a read path that checks nothing reads as an oversight and the next role added has to answer for it. |
| 2026-09-15 | M4.1 review resolution, second round (PR #9) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; each fix run against the unfixed code first; the concurrency test instrumented and re-run against the pre-fix shape three times | Pass | Three findings on the PATCH rework, all taken. **The merge sat outside the transaction that wrote it** — read in one, write in another, so two concurrent edits to different fields both read the same row and the second wrote its own field alongside stale copies of the rest. The read now happens inside the writing transaction, with the merge passed down as a closure. **What actually fixes it is the transaction boundary, not a row lock, and my first comments said otherwise.** `authorizeActor` already takes `LockWorkspace` on every mutation, which serializes every write in a workspace; removing `FOR UPDATE` from the task read changes nothing. That is why the first version of the concurrency test passed against code with no task lock at all. `LockTaskForUpdate` is kept for the reason `LockAgentForUpdate` is — the transaction states the row it depends on rather than relying on the workspace lock staying this coarse — and the comments now say that instead of claiming credit. **The concurrency test needed two goes, which is the more useful lesson.** The first passed against the unfixed code: instrumenting the interleaving showed the two patches never overlapped, because pgxpool creates its second connection lazily and the handshake outlasted the overlap window. Warming the pool first makes it fail on every run against the old shape. A concurrency test nobody has watched fail is not evidence of anything. **Two smaller ones:** an explicit `null` in `title`, `body` or `status` was read as an omission and answered 200 — `repository_id` is the only nullable field and the rest are refused now; and the contract oversold clearing a repository, which does not change the status, so a ready task needs `status: draft` in the same request — as the test had been sending all along while the description implied otherwise. **Deferred, for a maintainer:** optimistic concurrency on tasks. That is the stale-client-view problem rather than this one, and it needs a version column and a contract change to carry it. `workspaces.Rename` is the precedent if it is wanted. |
| 2026-09-14 | Branch Operations, Unit M3.3 — fourth review round (PR #8) | Local dev, macOS arm64, Go 1.26.8 | `make ci`; `make test-integration` | Pass | One finding, valid, and a misreading of Git's rules rather than a logic slip. `git-check-ref-format` forbids a ref that *is* the single character `@`; the validator rejected `weave/@`, a path component named `@`, under a comment claiming it was that rule. The namespace prefix already makes a name of `@` alone impossible, so the case could only ever fire on a legal name. Removed, with `weave/@` added to the accepting cases and the reasoning recorded where the neighbouring `@{` rule is handled, so the two are not conflated again. |
| 2026-09-14 | Branch Operations, Unit M3.3 — third review round (PR #8) | Local dev, macOS arm64, Go 1.26.8 | `make ci`; `make test-integration`; the new handler test run against the pre-fix handler to prove it catches the defect | Pass | One finding, and it was the previous round's fix stopping one layer short. The service was changed to return the branch *alongside* the error when an audit write fails, so neither the irreversible write nor the missing record would be hidden — and the HTTP handler discarded the branch, mapped the unrecognised error to a bare 500, and told the caller nothing had happened while a ref had just been written to a customer's repository, unnamed. **The service test asserting the return value passed throughout.** That is the lesson worth keeping: asserting a value one layer below the behaviour proves the layer below and nothing about what a caller receives. The fix adds `ErrAuditNotRecorded` so the condition is identifiable, answers 201 with the branch because that is what happened, and logs the gap at error level with workspace, actor, repository, branch, SHA and request id — the audience for an unrecorded audit is an operator, not the API caller. The new test lives at the transport layer and was run against the pre-fix handler first: 500 before, 201 after. |
| 2026-09-14 | Branch Operations, Unit M3.3 — second review round (PR #8) | Local dev, macOS arm64, Go 1.26.8; live `curl` of GitHub's branches and rules endpoints with literal and percent-encoded slashes | `make ci`; `make test-integration`; empirical check of slash handling against the real API | Pass | Nine findings: seven valid, one wrong on evidence, one left as a judgement. **Two were serious and both came from the previous round's own fix.** Generating a branch name when none was supplied made every attempt produce a different name, so a retry after a lost response created a *second* branch — the operation was idempotent by name while quietly making the name non-deterministic. The endpoint now requires a name; generation stays in the domain for M4, where a session id supplies stable identity. And the audit-repair path added last round would record `branch.created` against the retrying member for *any* matching branch, including one created by hand — nothing distinguishes an interrupted Weave attempt from an unrelated branch at the same commit. It was removed: a missing audit row is a gap, a false one is a false statement about a person in an append-only trail. An unrecordable creation now returns the branch *and* an error naming it, so neither fact is hidden. `HasAudit` also read `audit_events` outside the tenant transaction, where forced RLS matches no row — it would have reported every retry as unrecorded; removing the repair removed that too. **One finding was wrong and is recorded as such:** slashes in a branch name were said to require `%2F` encoding, which would have broken every `weave/` branch. Tested against the real API on an existing branch with a slash — `branches/feat/m3.3-branch-operations` returns 200 literally and percent-encoded alike, so both forms resolve. Also fixed: `@{` rejected locally, the 404 causes documented, and a test body read with `io.ReadAll` rather than one `Read` of `ContentLength`. |
| 2026-09-14 | Branch Operations, Unit M3.3 — review round (PR #8) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration` | Pass | Two findings, both valid, both defeating an invariant the unit claimed to enforce. **Protection was checked only on branches that exist.** A ruleset matches patterns, so one covering `weave/*` governs every branch this unit creates and none of them would have been found by reading an existing branch's `protected` flag — invariant 11 would have been satisfied everywhere it was tested and violated everywhere it mattered. Rules are now read by name through `GET /repos/{o}/{r}/rules/branches/{branch}`, which GitHub documents as answering for names that do not exist. It **fails closed**: an unanswerable protection check refuses the write, for the same reason absent tenant context matches no row, and a test proves creation does not proceed when the rules call errors. **An audit could be lost permanently.** Creation and its audit are two writes to two systems and GitHub goes first, being the one that cannot be rolled back; if the audit then failed, the retry landed on the already-exists path, which had nothing to create and did not record anything — leaving an irreversible write to a customer's repository unattributable. A retry now completes a record its first attempt lost, and a third attempt does not duplicate it. The refusal to widen the rules check beyond `creation`, `update`, `deletion` and `non_fast_forward` is deliberate: organisation-wide rulesets requiring signed commits or review are common and do not stop a working branch existing, so refusing on those would make the feature unusable in most governed repositories. |
| 2026-09-14 | Branch Operations, Unit M3.3 (`context/features-specs/09-branch-operations.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci` (now including `tidy-check`); `make test-integration`; live `curl` of the new route unauthenticated; grep of the API log for token and key material | Pass, with one gap | The M3.2 route-coverage gate earned itself on its first real use: adding the route made the test fail immediately with "served but the contract does not describe it", before the contract was written. Nine service tests cover the three outcomes an existing branch can have — idempotent success at the requested base, conflict elsewhere, refusal when protected — plus a missing base, a missing `contents:write` named rather than relayed, a viewer refused before reaching GitHub, and invalid names rejected before any request leaves the process. The withdrawn-repository test proves reach is checked against GitHub rather than our records: the stored row still says granted, GitHub no longer lists it, and the write is refused. A control test creates *from* a protected base, so the protection test cannot pass by refusing everything. Adapter tests fake GitHub at the HTTP boundary and pin what is actually sent — a fully qualified `refs/heads/` ref on creation and a bare name elsewhere, slashes preserved through path escaping, and a 422 saying "Reference already exists" translated while every other 422 stays an error. Domain tests reject 22 malformed names, with a control accepting ordinary ones. The route returns 401 unauthenticated and the API log contains no token or key material. **Gap: unverified against a real repository** — the branch has not been created on GitHub, which needs the operator. |
| 2026-09-13 | Generated API Client, Unit M3.2 (`context/features-specs/08-generated-api-client.md`) | Local dev, macOS arm64, Node 23.11.0, openapi-typescript 7.13.0 | `make ci` (now including `contracts-check`); `make test-integration`; deliberate contract edits to prove each gate fails | Pass | The premise was verified rather than asserted: removing `installation_id` from the contract and regenerating produces compile errors at exactly the two grouping sites in `github-section.tsx` — the code that silently grouped every repository under `undefined` during M3.1 and took a build-timestamp comparison to diagnose. The drift check was proven by changing the contract without regenerating, and the route-coverage test by adding a route with no contract path and a contract path with no route; all three fail as intended and pass when restored. **No disagreement was found between the hand-written types and the contract** — every one matched, so the migration surfaced nothing wrong on either side. Generated output is in `.prettierignore`, because formatting it would fight the generator: prettier would rewrite the file, regeneration would undo that, and the drift check would then fail every run for a reason unrelated to the contract. **Known limitation, stated in the test's own comment:** route coverage checks only that a path exists on both sides, never that request and response shapes agree. |
| 2026-09-14 | Generated API Client, Unit M3.2 — review round (PR #7) | Local dev, macOS arm64, Node 23.11.0 | `make ci`; `make test-integration`; deliberate contract and source edits to prove each gate fails | Pass | Seven findings, five valid and two already fixed by the tidy commit. The substantive one was mine to have caught: response *envelopes* were still hand-written — `{ repositories: Repository[] }`, `{ install_url }`, the list wrappers — so renaming a key in the contract would still have compiled and broken at runtime. That is the exact failure the unit exists to remove, so it had closed half the hole and claimed the whole one. Envelopes now derive from the generated `operations`, `ListResponse` is gone, and renaming the `repositories` key produces a compile error. The route-coverage test compared paths only, so adding a `DELETE` to a path described for `GET` passed while serving an undescribed, typeless endpoint; it now compares method-and-path pairs, proven by adding exactly that. Extraction was also matching any route-shaped literal rather than registration calls, so a path left behind after its registration was deleted would have counted — proven fixed by adding a bare `"GET /v1/ghost"` string and watching it be ignored. |
| 2026-09-11 | **Signed-in browser flow, end to end (M2.2, M2.3 and M3.1)** | Local dev, macOS arm64, real Clerk instance, real GitHub App `weave-ai-dev-jaggle`, two real GitHub accounts | Browser: sign in, create a workspace, invite, accept, connect GitHub on a personal account and an organisation, sync repositories; `psql` verification of the resulting audit trail and rows | Pass | **Closes the gap carried since M2.2.** The audit trail for the real workspace records `workspace.created`, `workspace.invitation.created`, `workspace.invitation.accepted` and `github.installation.connected` twice; 113 repositories synced for the personal account and 60 for the organisation. Four defects surfaced that no automated test caught, which is the point of having done it: the install callback exceeded the web client's five-second budget because `CompleteInstall` fetched the installation twice and listing 113 repositories takes 3.3s — and it failed *after* the write, reporting failure for work that had succeeded with the state value already spent; the repository list was rendered flat with a hard-coded `connected[0]` sync control, so of two installations only one could ever be refreshed and a half-empty connection looked complete; the sync action was wrapped in a client closure rather than bound, so `revalidatePath` invalidated the server cache while the router was never told, making a working button look dead; and a failed repository fetch was swallowed into an empty list, reported as "no repositories yet" — a claim about GitHub when the cause was ours. Also recorded: grepping a compiled binary for a JSON tag is not evidence it is current, because the string was already present from unrelated fields; build timestamps against commit timestamps settled it. |
| 2026-09-11 | GitHub App Installation, Unit M3.1 (`context/features-specs/07-github-app-installation.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2; real GitHub App `weave-ai-dev-jaggle` (id 4906626) | `make ci`; `make test-integration`; `make migrate-up` / `migrate-down` / `migrate-up`; live `curl` of the webhook endpoint with genuine and forged HMAC signatures; `psql` as `weave_app` against the three new tables | Pass, with one gap | Credentials verified against GitHub's own API before any code depended on them: an App JWT minted from the private key was accepted by `GET /app`, confirming the App id and key are a matching pair, the slug, and that permissions are exactly `contents:write`, `metadata:read`, `pull_requests:write`. Webhook endpoint exercised live against the running API: a genuine signature is accepted (204), a replayed delivery id produces exactly one row, an unknown event type and an installation we have no record of are both ignored rather than failing, and absent, wrong and post-signing-altered signatures are all refused (401). The three new tables return nothing to `weave_app` without tenant context, and nothing when another tenant's context names this workspace explicitly. Integration tests prove an installation cannot be bound twice and that the refusal leaves the first binding untouched, that another tenant's installation is absent rather than forbidden, that a deselected repository is withdrawn and kept rather than deleted, that a rename keeps one row because the GitHub id is the identity, and that a suspension keeps the repository records. GitHub is faked at the HTTP boundary, which is what makes the App JWT itself assertable — `iat` backdated against clock skew, validity inside GitHub's ten-minute maximum. Gap subsequently closed the same day: the signed-in browser flow was walked end to end — see the row above — including both install round trips and the repository sync. |
| 2026-09-11 | Row-Level Security, Unit M3.0 — second review round (PR #5) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-down` / `migrate-up`; full CI on the pull request | Pass | One further finding, applied although its stated consequence did not hold: `weave_invitation_preview_by_token` matched on token hash alone, but `Preview` refuses expired, revoked and accepted tokens before reaching the store, and a test already covered all three. The check is now repeated inside the function regardless, because it is a `SECURITY DEFINER` hole and what it returns is reachable by any future caller that forgets the gate. The new test calls the store directly, skipping the service, with a usable-token control so it cannot pass vacuously. `now()` inside the function is a second clock, accepted here because skew can only withhold a preview the service would have allowed, never disclose one it would have refused. Also repaired ADR-012, which had never received content an earlier commit message claimed for it — two edits written from the same unmodified string, the second discarding the first, leaving the file saying to keep to one `SECURITY DEFINER` function while the code had three. |
| 2026-09-11 | Row-Level Security, Unit M3.0 — review round (PR #5) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-down` / `migrate-up`; `psql` as `weave_app` running the pre-fix preview query; full CI on the pull request | Pass | Ten findings from CodeRabbit and greptile, all verified against the code and all valid. One was a live defect, found independently by both: the invitation preview reads the invitation joined to its workspace, and the viewer is by definition not yet a member, so with no workspace context both policies matched nothing and every legitimate invitee was told their invitation did not exist. Proved rather than assumed — the old query as `weave_app` returns 0 rows. **The suite could not have caught it**: the existing integration tests connect as the owner, a superuser locally, so they bypass RLS and test the application's filtering rather than the policies. The regression test runs as the application role. Also fixed: `weave_app`'s attributes are now normalised unconditionally, since `CREATE ROLE` runs only when absent and a leftover role carrying `BYPASSRLS` would bypass every policy while looking correct; the three `SECURITY DEFINER` functions are owned by a `NOLOGIN BYPASSRLS` role, because owned by the table owner they would be filtered by the policies they exist to look past wherever the owner is subject to `FORCE` — invisible locally, fatal to invitations elsewhere; `EXECUTE` is revoked from `PUBLIC` before being granted; readiness probes the application pool and asserts its role cannot bypass RLS, replacing a check that had been run by hand; `migrate app-role` refuses any username but `weave_app`; and `make test-integration` fails when either URL is empty rather than skipping every RLS test and reporting success. CI also now migrates before the stack health check — the job had been starting the API against an unmigrated database, which only passed because readiness probed the owner pool alone. |
| 2026-09-11 | Row-Level Security, Unit M3.0 (`context/features-specs/06-row-level-security.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-up` / `migrate-down` / `migrate-up`; direct `pgxpool` connection as `weave_app` asserting `rolsuper`/`rolbypassrls`; live API health probe | Pass, with one gap | Six RLS tests connect as `weave_app` and **refuse to run at all if the role can bypass RLS** — a test that silently ran as superuser would pass while proving nothing. They prove: an unfiltered `SELECT id FROM workspaces`, with the workspace filter removed on purpose, returns only the current tenant's rows; naming another tenant's `workspace_id` explicitly returns nothing; with no context set every tenant table returns nothing; and, on a deliberately pinned single connection, a transaction that set context leaves none behind for the next — which is the pooling hazard the whole design turns on. Writes are constrained too, and `weave_invitation_by_token` returns exactly one row and cannot be widened. Every pre-existing test passes unchanged, so the convention and the policies agree. `APP_DATABASE_URL` confirmed to connect as `weave_app` with `rolsuper=false, rolbypassrls=false`. Correction to ADR-010's framing: `FORCE` does **not** subject a superuser or `BYPASSRLS` role — the local `weave` owner is a superuser and still reads every row — so the control carrying isolation is the application's role, not `FORCE`. **Gap: the signed-in browser flow remains unverified**, as with M2.2 and M2.3. |
| 2026-09-11 | Workspace Invitations, Unit M2.3 (`context/features-specs/05-workspace-invitations.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-up`; `curl` of all five invitation routes unauthenticated; grep audit of the API log and of every logger and JSON tag for token material | Pass, with one gap | Domain tests cover the derived status (including terminal states winning over expiry), token entropy and URL-safety across 200 samples, and that the stored hash — raw or hex-encoded — cannot be presented as a token. Integration tests against real PostgreSQL prove: eight concurrent accepts of one token create exactly one membership; unknown, expired, revoked, accepted and empty tokens are indistinguishable from both preview and accept; a forwarded link is refused for the wrong recipient and still works for the right one; email matching is case-insensitive; one outstanding invitation per address, with an expired one superseded rather than blocking and its row kept; re-inviting after removal restores access at the newly invited role; an admin cannot invite an owner; and an invitation id from one tenant cannot be revoked through another. All five routes return 401 unauthenticated while `/health/*` stays public. The API log contains no token material — the only matches are the phrase "bearer token" in auth-failure messages. **Gap: the signed-in browser flow is unverified**, as with M2.2. |
| 2026-09-10 | Workspaces and Membership, Unit M2.2 — review round (PR #3) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; direct `psql` probes of the slug constraint before and after the fix; `go run` probe of int32 narrowing; full CI on the pull request and on `main` | Pass | Eight findings from greptile and CodeRabbit, all verified against the code and all valid. Two were serious: `member:manage` alone let an admin assign the `owner` role and collect `billing:manage` (fixed with a permission-subset grant rule); and `citext` overrides `~` to be case-insensitive, so the slug CHECK accepted `UpperCaseSlug` — proved by inserting one before the `::text` cast and watching it succeed, then fail after. Also fixed a time-of-check/time-of-use window where a removed or demoted actor could still land a privileged write (mutations now re-read the actor's membership under the workspace lock), `int32` truncation that let `4294967297` masquerade as version 1, slug-suffix overflow, and trailing-JSON acceptance. Every fix carries a regression test, including both TOCTOU cases against real PostgreSQL. **Still unverified: the signed-in browser flow**, which needs a GitHub sign-in only the operator can perform. |
| 2026-09-10 | Workspaces and Membership, Unit M2.2 (`context/features-specs/04-workspaces-and-membership.md`) | Local dev, macOS arm64, Go 1.26.8, PostgreSQL 17.2 | `make ci`; `make test-integration`; `make migrate-up` / `migrate-down` / `migrate-up`; `psql` probes of the append-only triggers; `curl` of all seven routes unauthenticated; browser check of the signed-out redirect | Pass, with one gap | Authorization matrix pinned by an exhaustiveness test that fails the build when a role or permission is added without deciding every pairing, plus a hand-written expectation table so the matrix cannot be changed without changing the test. HTTP tests cover every workspace-scoped endpoint × every role: a non-member receives 404 on all five (never 403), an unknown workspace is byte-identical to one that is not yours, and a member lacking a permission receives 403. Integration tests against real PostgreSQL prove cross-tenant reads return nothing, the last owner cannot be demoted or removed, two concurrent owner demotions leave exactly one owner (the reason the store takes a row lock), a stale version is rejected, refused changes write no audit row, and audit rows survive deleting the workspace they describe. Append-only is enforced by the database: UPDATE, DELETE and TRUNCATE are all rejected by trigger. All seven routes return 401 unauthenticated while `/health/*` stays public. **Gap: the signed-in browser flow is unverified** — it needs a GitHub sign-in only the operator can perform. |
| 2026-09-10 | Authentication Baseline, Unit M2.1 (`context/features-specs/03-authentication-baseline.md`) | Local dev, macOS arm64, Go 1.25.14, Node 23.11.0, PostgreSQL 17.2 | `make ci` (now including `sqlc-check` and `lint-go`); `make test-integration` against the compose database; `make migrate-up`; `psql \d users`; `curl` against a running API for every auth path; browser check of the web shell | Pass, with one gap | Migration applied and schema matches the spec. Integration tests against real PostgreSQL prove 16 concurrent first requests create exactly one row, that citext makes email lookup case-insensitive, and that empty profile fields store as NULL. Verifier tests use a locally generated RSA key and an httptest JWKS server — no Clerk network call — covering valid, expired, wrong issuer, wrong audience, unpublished key, tampered payload, malformed, empty, missing-email, and HMAC algorithm confusion. Live API: `/health/*` public and 200; `/v1/me` returns 401 with the standard envelope and an echoed `X-Request-Id` for missing, malformed and wrong-scheme credentials; a well-formed RS256 token against an unreachable issuer returns 503, while `alg:none` returns 401 without any network call. Gap subsequently closed: a real Clerk application was linked, `session.claims` configured, and sign-in with GitHub confirmed to create exactly one `users` row carrying the real email, with `GET /v1/me` returning it. |
| 2026-09-10 | Repository Foundation, Unit M1.1 (`context/features-specs/02-repository-foundation.md`) | Local dev, macOS arm64, Node 23.11.0, Go 1.25.14, Docker 28.1.1, Compose 2.35.1 | `make check-prereqs`; `make ci` (gofmt, prettier, go vet, eslint, tsc, `go test -race`, go build, next build); `golangci-lint run`; `govulncheck`; `npm audit --audit-level=high`; gitleaks via Docker; `make dev` from a clean checkout (no `.env`, no containers, no volumes); `make health`; per-dependency outage probes; `curl` of both probes and the web shell | Pass | Monorepo layout created and the Next.js app moved to `apps/web` via `git mv` (history preserved); dark-theme tokens verified intact in the browser (`--background #090b10`, `--primary #8b7cff`, `--card #131722`, `--ring #a99fff`). Go API serves `/health/live` (200, no dependency I/O) and `/health/ready` (200 when all up; 503 naming the failing dependency, verified by stopping postgres, redis and nats in turn). Compose stack reports all five services healthy. Config validation fails fast listing every missing variable at once. Local gates all green; **the CI workflow file itself is unverified until first push** — each gate's command was verified locally instead. |
| 2026-09-10 | M1.1 review follow-up (CodeRabbit on PR #1) | Local dev, macOS arm64, Docker 28.1.1 | `make ci` (gofmt, prettier, go vet, eslint, tsc, `go test -race`, go build, next build); `golangci-lint run` (0 issues); `govulncheck` at the newly pinned v1.7.0; gitleaks v8.30.1 in git mode; full compose stack up with `--wait`; both probes via `curl` and `scripts/health.sh` | Pass | Nine inline findings triaged: eight fixed, one rejected on evidence. Two fixes were measured rather than assumed — the NATS readiness probe returned in 10.01s before the change and 3.01s (the configured `ReadinessTimeout`) after, with the NATS container paused to simulate an open-but-unresponsive server; and `scripts/health.sh` was reproduced building `http://localhost127.0.0.1:8080` from a host-qualified `API_HTTP_ADDR` before the fix. Compose publishers confirmed bound to 127.0.0.1 only. |
| 2026-09-10 | Design System and UI Primitives (`context/features-specs/design-system.md`) | Local dev, Node 23, Next.js 16.3.4 | `npx tsc --noEmit`, `npx eslint .`, `npm run build`, manual browser check of Button/Card/Dialog/Input/Tabs/Textarea/ScrollArea at `http://localhost:3000` | Pass | shadcn/ui installed (`style: base-nova`, Base UI primitives, not Radix); Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea added via `npx shadcn add`; `lucide-react` installed; `lib/utils.ts` exports `cn()` from the official `cn` package; `app/globals.css` dark-theme tokens replaced with the hex values from `ui-context.md` (background, foreground, card, popover, primary, secondary, muted, accent, destructive, border, ring, sidebar, chart). Verified visually via a temporary route (removed after verification) — correct dark surfaces/borders, purple primary accent, working dialog with backdrop blur, no hydration errors, no default light styling. |
| 2026-09-10 | Pull request docstring coverage repair | Local sandbox, Node 24.14.1, Go 1.25.14 | `make fmt-check lint typecheck test` | Pass | Documented all 52 top-level functions in the M1.1 pull-request diff. The external docstring coverage gate remains pending its next pull-request run. |

## Session Notes

- **M5.2's four recorded decisions.** (1) **System actor for branches.** `CreateBranchAsSystem` skips the `repository:manage` check and nothing else — reach, `contents: write`, protection and rulesets are enforced exactly as for a member. Authorization happened when a member with `session:create` created the session; re-checking a role the system does not hold would either always fail or require borrowing the creator's identity. It takes its workspace and repository only from the session row, and `TestNoHandlerActsAsTheSystem` keeps it out of HTTP. (2) **The SHA lives on `sessions`, not on a transition row**, because cutting a branch happens *during* `provisioning` and is not a state change; a transition to carry it would record a move that never happened. It is write-once twice over: the store compares under the row lock, and a trigger refuses any other path. The version is not bumped, so a member's pause or cancel does not conflict with it. (3) **The audit is written in the same transaction as the SHA, not best-effort beside it** — a deliberate improvement on the spec, which proposed keeping the separate event as a fallback. Neither can now exist without the other. The one window left is GitHub making the ref and the process dying before that transaction; the retry then records the branch as `session.branch_found`, which is true, rather than as created, which it cannot prove — M3.3's rule that a false audit is worse than a missing one. (4) **The worker gains Redis and the GitHub App key, and readiness does not change.** Both are per-activity dependencies under a bounded retry, and a session whose branch cannot be cut is failed with a reason; a worker marked unready during a GitHub outage would be restarted for something it cannot fix and would stop draining the outbox. The worker does not take the webhook secret or the slug.
- The six context documents are the implementation baseline.
- Build M1.1 before implementing product features.
- Use a fake deterministic agent adapter before integrating a paid provider; this verifies orchestration, events, approvals, and UI independently.
- Do not run untrusted code in the control-plane process or through a host Docker socket.
- Update this file when M1.1 begins, after every meaningful implementation unit, and whenever a blocking decision is discovered.
- Design system components have been moved from the repository root to `apps/web/` as part of M1.1, resolving the temporary placement noted during the design-system unit.
- **Local dependency ports are offset from their defaults** (PostgreSQL 55432, Redis 56379, NATS 54222/58222, Temporal 57233/58233). This was found the hard way: a host Redis already owned 6379, the compose stack still reported healthy because container healthchecks run inside the container, and on macOS a host process bound to `127.0.0.1` wins over Docker's `0.0.0.0` publish — so readiness passed while the API silently talked to the wrong datastore. Do not "simplify" these back to the standard ports.
- **`go.mod` carries both a `go` directive (1.25.0) and a `toolchain` directive (go1.25.14).** They differ deliberately: 1.25.0 is the minimum language version the dependencies require, and go1.25.0 ships standard-library CVEs that `govulncheck` flags. `golang.org/x/text` was also bumped to v0.39.0 for the same reason. `govulncheck` is clean; re-check it when bumping either.
- Go commands use explicit package selectors (`./services/... ./internal/...`) rather than `./...`, because npm workspaces hoist `node_modules` to the repository root and one transitive npm package vendors a `.go` file that would otherwise be treated as part of this module.
- The web workspace has **no unit tests**; the `test` gate covers Go only. A web test runner should be added with the first stateful UI logic (reducers, event merging), not before.
- **`/health/ready` probes PostgreSQL, Redis and NATS and deliberately not Temporal — settled in M5.1 rather than still open.** The question was whether the session workflow would make Temporal a hard serving dependency of the API. It does not: the publisher runs in the worker, so the API neither starts workflows nor holds a Temporal connection, and failing its readiness for a Temporal outage would take the product offline for requests that never touch it. The worker is where that dependency lives, and it treats an unreachable Temporal as fatal at startup rather than as something to report — it cannot do its job without one. Do not add a Temporal probe to the API without first moving something that needs Temporal into it.
- Readiness responses report only `ok`/`unavailable` per dependency; the underlying error is logged, never returned, because probe output is unauthenticated and dependency errors embed hostnames and connection strings.
- **An invitation token is a bearer capability (ADR-011).** Only its SHA-256 is stored, so it is shown once and cannot be re-displayed; the UI says so. Unknown, expired, revoked and already-accepted all return one error, deliberately — do not "improve" the message to distinguish them, because that turns token guessing into a search with feedback. The accepting account's email must match the invited address, which is what makes a forwarded link safe.
- **Acceptance is a single conditional UPDATE, not a read-then-write.** That statement is what makes the token single use; eight concurrent accepts produce exactly one membership, and there is a test for it.
- **`make dev` needs `NEXT_PUBLIC_APP_URL`** to build invitation links. The API cannot build them because it does not know the browser-facing host.
- **The authorization matrix in `internal/domain/authorization.go` is the only place a role implies anything.** Nothing else compares a role name. `TestMatrixIsExhaustive` fails the build if a new role or permission is added without deciding every pairing — including the denials, written out as `false`, because an omission denies at runtime but records no decision.
- **Non-member is 404, member-without-permission is 403.** A 403 for a workspace that exists but is not yours confirms its existence and turns identifier guessing into tenant enumeration. A test asserts the two responses are indistinguishable; do not "improve" the non-member case to 403.
- **The last-owner rule is decided under `SELECT ... FOR UPDATE` on the workspace row.** Without the lock two concurrent demotions each observe two owners and each proceed, leaving none — a workspace nobody can administer. There is a test for exactly that race.
- **`audit_events` has no foreign keys and two triggers.** No FK so records outlive the workspace they describe; a row trigger blocking UPDATE and DELETE, and a statement trigger blocking TRUNCATE, because a row trigger alone leaves the table erasable in one statement.
- **`make ci` fails locally on `sqlc-check` until generated code is staged.** The gate runs `git diff --exit-code` against the working tree, which is correct in CI where everything is committed. Run `git add` first, or expect one confusing failure.
- **A Clerk instance needs `infra/clerk/session-claims.json` applied before sign-in works.** Clerk's default session token carries no email, so the API rejects an otherwise-valid credential with `token carries no email claim` — provisioning cannot create a user from it. Apply with `clerk config patch --file infra/clerk/session-claims.json`. The alternatives were a Clerk secret key in the Go API for Backend API lookups, or nullable emails; adding the claim keeps the secret out of the API entirely. `CLERK_ISSUER` is not written by the CLI (it is ours) and is derived from the publishable key, which encodes the frontend domain. Google sign-in is still enabled on the dev instance and the spec says GitHub only — disable with `clerk config patch --json '{"connection_oauth_google":{"enabled":false}}'`.
- **`make dev` builds the API to `bin/api` and runs it directly, never via `go run`.** `go run` compiles to a temporary binary and execs it as a child, so the PID it reports is the wrapper: killing it leaves the server running, reparented to init, still holding port 8080, and the next `make dev` fails with a bind error that looks unrelated. `dev.sh` also enables job control and signals process groups, because `npm run dev` is three processes deep (npm, next, next-server) and signalling only the wrapper orphans the server on port 3000. It refuses to start at all when either port is occupied, naming the process and the kill command.
- **`apps/web/.env` is a symlink to the root `.env`, created by `make dev` / the Makefile's `.env` target.** Next.js reads env files only from its own directory, and neither exporting the variables into the process nor calling `loadEnvConfig` from `next.config.ts` reaches the edge runtime that `proxy.ts` runs in or the `NEXT_PUBLIC_` inlining the client bundle needs. Both were tried and both failed; the symlink is what works. It is gitignored, so a fresh clone gets it from the task runner.
- **The migration runner is `services/migrate`, not the goose CLI.** Installing the CLI links every database driver goose supports (ClickHouse, YDB, libsql, MySQL); it filled the disk mid-build. Driving goose as a library reuses the pgx driver already present. Migrations run as their own step, never at API startup.
- **Web route protection lives in `app/(app)/layout.tsx`, not in path patterns.** Clerk deprecated `createRouteMatcher` because path matching can diverge from how Next.js resolves routes and leave protected resources reachable. The layout protects everything nested in the route group, mirroring how the API wraps its whole `/v1` subtree — in both cases a new route is protected by where it sits, not by remembering.
- **Clerk Core 3 removed `<SignedIn>` / `<SignedOut>`;** use `<Show when="signed-in">`. The old names are still exported and throw at runtime, so this fails at render rather than at compile.
- Go commands and gates now include `sqlc-check`, which regenerates and fails on a diff. A Homebrew-installed sqlc shadowed the pinned one during this unit and generated output at the wrong version, which is exactly what that gate catches.
- Only the directories the M1.1 spec enumerated were created. `docs/adr/`, `docs/runbooks/`, `test/fixtures/` and `apps/web/features/` are named in `code-standards.md` but were deliberately not scaffolded — create each with its first real occupant rather than as an empty placeholder.
- `app/globals.css` now carries only the dark-theme token values from `ui-context.md`; light theme and a theme toggle are not implemented. `ui-context.md` requires both themes for the shipped product, so this must be completed before M1.1's "Next.js application shell with design tokens" is considered done, or before any user-facing light/dark toggle ships.
- **`natsProbe` uses `conn.FlushWithContext(ctx)`, never `conn.RTT()`.** `RTT()` flushes with a hardcoded 10s timeout that ignores the caller's context, and `health.Ready` waits for every probe, so one unresponsive NATS server held the whole readiness response past `ReadinessTimeout`. Measured: 10.01s before, 3.01s after.
- **Every Compose port is published on `127.0.0.1`, not the Compose default of all interfaces.** The stack uses development credentials or none; an unqualified publish bypasses the host firewall. This is separate from, and additional to, the port offsets noted above.
- **The check-prereqs Go check measures the *resolved* toolchain, not the base install.** `scripts/check-prereqs.sh` runs `go version` from the repository root, where `GOTOOLCHAIN=auto` has already applied go.mod's `toolchain go1.25.14`, so a developer whose base Go is older still passes and still builds against the pin. Verified on a machine whose base Go is 1.23.5. A review comment asserted the opposite (that the prereq gate blocks the `GOTOOLCHAIN=auto` fallback) and was rejected on this evidence — do not "fix" the docs to match that claim without re-testing.
- **`apps/web/components/ui/tabs.tsx` carries one deliberate deviation from generated shadcn output:** `orientation` is forwarded to `TabsPrimitive.Root`, not just rendered as `data-orientation`. Upstream swallows it, so vertical tabs got correct styling but horizontal arrow-key navigation and `aria-orientation`. The file comments the deviation; re-apply it if the component is regenerated.
- **`versions.env` pins the security scanners** (`GOVULNCHECK_VERSION`, `GITLEAKS_VERSION`) so a gate result is reproducible. govulncheck is held at 1.7.0 rather than 1.8.0 because 1.8.0 requires go1.26 and would make CI download a second toolchain purely to build the scanner.
- **`.gitleaks.toml` allowlist regexes match exact synthetic values only.** Note that the committed development credentials are low-entropy enough that gitleaks' default ruleset does not flag them at all — verified by scanning with the allowlist block removed, which still reports no leaks. The allowlist is defence-in-depth against a future rule change, not currently load-bearing.
- shadcn's current CLI (`shadcn@4.x`, `style: base-nova`) generates components on Base UI primitives, not Radix, even though `architecture.md` and `ui-context.md` say "Radix primitives." Behavior and accessibility semantics are equivalent for the primitives installed so far; flag this if a future unit relies on Radix-specific APIs.

## Last Updated

2026-09-25 — Unit M5.2 merged as 29bbba1 (PR #17), squash-merged so an accidentally committed 47 MB binary stays out of `main`'s history. Two review rounds and five findings: cut and record split into separate activities so a branch GitHub made is never retried away or called an outage; GitHub's refusals named rather than retried; rate limits kept retryable — a regression the first round's fix introduced and the second caught. Next: M5.3, event contracts and ingestion. Previously, 2026-09-25 — Unit M5.2 built and in review: the session workflow cuts each session's branch while provisioning, records its commit on the session with its audit in one transaction, and fails the session with a named cause when GitHub refuses. **The first time Weave writes to a customer repository on its own**, and observed live: `weave/work-on-authentication-3xyp37ao` in `jigmetnamgyal/thuenlam` at the SHA recorded on the session. Next: M5.3, event contracts and ingestion. Previously, 2026-09-18 — Unit M5.1 merged as aa59c70 (PR #16) after two review rounds: a session created through the API now reaches a terminal state on its own, and the outbox claim protocol M4.2 designed is finally exercised. The review's own theme was one finding repeated three times — a write to a claimed row that was not fenced by the claimant — the third of them in the test helper written to protect other tenants' claimed rows. Next: M5.2, branch creation in the workflow, which closes the oldest gap in the project — no branch has ever been created against a real repository. Previously, 2026-09-17 — Unit M5.0 merged as 97d5cb2 (PR #14) after two review rounds and eight findings: `POST /sessions` is safe to retry, which was the gate in front of the rest of M5. Next: M5.1, the outbox publisher and the first workflow, specced in `context/features-specs/14-outbox-publisher-and-workflow.md`, which owes the two claim tests M4.2 could not write and answers the Temporal readiness question carried since M1.1. **Still outstanding: the M4.3 browser walk and M3.0's unrecorded verification**, both of which one signed-in session would close.